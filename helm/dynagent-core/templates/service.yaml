apiVersion: v1
kind: Service
metadata:
  name: {{ include "dynagent-core.fullname" . }}
  labels:
    {{- include "dynagent-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dynagent-core.selectorLabels" . | nindent 4 }}
